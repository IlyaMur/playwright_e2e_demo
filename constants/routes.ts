export enum ShopRoute {
  GALLERY = '/',
  SIGNUP = '/signup',
  ACCOUNT = '/my-account',
  LOGIN = '/login',
  BASKET = '/basket',
  DELIVERY_DETAILS = '/delivery-details'
}

export enum APIRoute {
  SIGNUP = 'api/signup',
}
